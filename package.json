{
  "name": "notigas-web",
  "version": "1.0.0",
  "description": "NOTIGAS Web App PWA",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node scripts/check_syntax.js && node scripts/check_runtime.js && node scripts/test_rls_security.js",
    "build": "node scripts/check_syntax.js && node scripts/check_runtime.js && node scripts/test_rls_security.js"
  },
  "keywords": [
    "notigas",
    "pwa",
    "geospatial",
    "delivery"
  ],
  "author": "NOTIGAS",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
