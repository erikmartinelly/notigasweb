{
  "name": "notigaswebdeploy",
  "version": "1.0.0",
  "description": "NOTIGAS Node.js Web App for Hostinger",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "echo 'Build OK - Vanilla JS requires no compilation'",
    "test": "node -c server.js && node -c js/app.js && node -c js/events.js && node -c js/state.js && node -c js/map.js && node -c js/orders.js && node -c js/auth.js && echo 'Syntax check OK'"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "jsdom": "^24.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
